import RulesPage from "../Rules/RulesPage";

// `rules` remains a backwards-compatible internal route for saved workspaces.
// Staff navigation and new next-action results use the clearer name.
export default RulesPage;
